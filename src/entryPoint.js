import "./styles.css";
import { greeting } from "./greeting.js";

console.log(greeting);

class Node {
  constructor(left, right) {
    this.left = left;
    this.right = right;
  }
}

class Tree {
  constructor() {
    this.root;
  }

  buildTree(array) {
    
  }
}